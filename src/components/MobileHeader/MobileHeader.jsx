import { Flex } from '@chakra-ui/react';
import React from 'react';

const MobileHeader = () => {
    return (
        <Flex w={'full'}>
           
        </Flex>
    );
}

export default MobileHeader;